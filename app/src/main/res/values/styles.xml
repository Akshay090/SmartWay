<resources>

    <!-- Base application theme. -->
    <!-- Light/Day Mode theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">#efefef</item>
        <item name="backgroundCardColor">#ffffff</item>
        <item name="backgroundPackageColor">#ffffff</item>
        <item name="textColor">#000000</item>
        <item name="textTitleColor">#808080</item>
        <item name="textSubTitleColor">#404040</item>
        <item name="tabColor">#000000</item>
        <item name="iconColor">#A9143C</item>
        <item name="tintColor">#000000</item>
        <item name="buttonColor">#A9143C</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- Night / dark  Mode theme. -->
    <style name="DarkTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#212121</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">#303030</item>
        <item name="backgroundCardColor">#424242</item>
        <item name="backgroundPackageColor">#424242</item>
        <item name="textColor">#ffffff</item>
        <item name="textTitleColor">#fcfcfc</item>
        <item name="textSubTitleColor">#fafafa</item>
        <item name="tabColor">#ffffff</item>
        <item name="iconColor">#A9143C</item>
        <item name="tintColor">#ffffff</item>
        <item name="buttonColor">#A9143C</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>

    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation" >@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
